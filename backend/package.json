{
	"name": "backend",
	"version": "1.0.0",
	"description": "",
	"main": "build/main.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "tsc && node build/main.js",
		"start-watch": "concurrently \"tsc -w\" \"nodemon build/main.js\""
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/socket.io": "^2.1.4",
		"concurrently": "^5.0.0",
		"nodemon": "^2.0.1",
		"typescript": "^3.7.2"
	},
	"dependencies": {
		"socket.io": "^2.3.0"
	}
}
